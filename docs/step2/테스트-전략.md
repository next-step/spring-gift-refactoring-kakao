# 2단계 테스트 전략

## 원칙

- **테스트 먼저**: 리팩토링 구현 전에 테스트를 작성하거나 수정한다.
- **실패 → 구현 → 통과**: 작동 변경은 실패하는 테스트를 먼저 작성하고, 구현 후 통과시킨다.
- **구조 변경은 기존 테스트가 안전망**: 구조만 바꾸는 경우 새 테스트 없이 기존 테스트가 그대로 통과해야 한다.
- **커밋 분리**: 테스트 추가/수정 커밋과 구현 커밋을 나눈다.

---

## Phase별 테스트 계획

### Phase 1: 트랜잭션 경계 세우기

| 단계 | 작업 | 파일 | 상태 |
|------|------|------|------|
| 테스트 | `fail_insufficientPoints_shouldNotSubtractStock` 추가 | OrderAcceptanceTest.java | **완료** |
| 구현 | `@Transactional` 적용 | OrderService.java | **완료** |
| 검증 | 위 테스트 통과 + 기존 테스트 전체 통과 | - | **완료** |

**테스트 시나리오**:
```
Given: 포인트 100인 회원, 상품가격 1000원, 재고 100개
When:  수량 1개 주문 시도 (가격 1000 > 포인트 100)
Then:  주문 실패 (400)
Then:  재고가 100개 그대로인지 확인
```

---

### Phase 2-1: GlobalExceptionHandler 전역 적용 + 예외 타입 통일

| 단계 | 작업 | 파일 | 상태 |
|------|------|------|------|
| 테스트 수정 | 기존 `500` 기대값을 `400`으로 변경 | OrderAcceptanceTest.java | **완료** |
| 구현 | GlobalExceptionHandler의 `assignableTypes` 제거하여 전역 적용 | GlobalExceptionHandler.java | **완료** |
| 검증 | 변경한 테스트 통과 + 기존 테스트 전체 통과 | - | **완료** |

**수정 대상 테스트 목록**:

| 테스트 | 변경 내용 |
|--------|----------|
| `CreateOrder.fail_insufficientStock` | `.statusCode(500)` → `.statusCode(400)` |
| `CreateOrder.fail_insufficientPoints` | `.statusCode(500)` → `.statusCode(400)` |
| `CreateOrder.fail_insufficientPoints_shouldNotSubtractStock` | `.statusCode(500)` → `.statusCode(400)` |

---

### Phase 2-2: 주문 후 위시리스트 정리

| 단계 | 작업 | 파일 | 상태 |
|------|------|------|------|
| 테스트 추가 | `success_removesWishAfterOrder` | OrderAcceptanceTest.java | **완료** |
| 테스트 추가 | `success_orderWithoutWish` | OrderAcceptanceTest.java | **완료** |
| 구현 | OrderService.createOrder에 위시리스트 정리 로직 추가 | OrderService.java | **완료** |
| 검증 | 새 테스트 통과 + 기존 테스트 전체 통과 | - | **완료** |

---

### Phase 3-1~3-5: 도메인 책임 되찾기

| Phase | 작업 | 안전망 | 상태 |
|-------|------|--------|------|
| 3-1 (AdminProduct→ProductService) | 구조 변경 — 새 테스트 없음 | ProductAcceptanceTest 16개 | **완료** |
| 3-2 (AdminMember→MemberService) | 구조 변경 — 새 테스트 없음 | MemberAcceptanceTest 8개 | **완료** |
| 3-3 (KakaoAuth→KakaoAuthService) | 구조 변경 — 새 테스트 없음 | 기존 테스트 전체 | **완료** |
| 3-4 (가격 계산 중복 제거) | 구조 변경 — 새 테스트 없음 | OrderAcceptanceTest | **완료** |
| 3-5 (인증 체크 중복 제거) | 구조 변경 — 새 테스트 없음 | 인증 관련 테스트 6개 | **완료** |

> 순수 구조 변경이므로 기존 테스트만으로 충분하다.

---

### Phase 5-1: 옵션 수정 API 추가

| 단계 | 작업 | 파일 | 상태 |
|------|------|------|------|
| 테스트 추가 | UpdateOption 테스트 6개 작성 | OptionAcceptanceTest.java | **완료** |
| 구현 | Option.update(), OptionService.updateOption(), OptionController PUT | Option.java, OptionService.java, OptionController.java | **완료** |
| 검증 | 새 테스트 6개 통과 + 기존 테스트 전체 통과 | - | **완료** |

**테스트 시나리오**:

| 테스트 | 시나리오 | 기대 결과 |
|--------|----------|----------|
| `success` | 유효한 이름/수량으로 수정 | 200, 수정된 값 반환 |
| `fail_productNotFound` | 존재하지 않는 상품 | 404 |
| `fail_optionNotFound` | 존재하지 않는 옵션 | 404 |
| `fail_duplicateName` | 같은 상품의 다른 옵션과 이름 중복 | 400 |
| `fail_invalidSpecialChars` | 허용되지 않은 특수문자 | 400 |
| `success_sameName` | 자기 자신의 이름 유지 (수량만 변경) | 200 |

---

### Phase 5-2: 카카오 알림 트랜잭션 분리

| 단계 | 작업 | 파일 | 상태 |
|------|------|------|------|
| 구현 | OrderTransactionService 추출, 카카오 알림을 트랜잭션 밖으로 이동 | OrderService.java, OrderTransactionService.java(신규) | **완료** |
| 검증 | 기존 주문 테스트 전체 통과 | - | **완료** |

> 순수 구조 변경이므로 기존 테스트만으로 충분하다.

---

## 요약: Phase별 테스트 변경량

| Phase | 새 테스트 | 수정 테스트 | 안전망 (기존) | 비고 |
|-------|----------|-----------|-------------|------|
| 1 (트랜잭션) | 1개 | 0 | 79개 | 완료 |
| 2-1 (예외 핸들러) | 0 | 3개 | 79개 | 완료 |
| 2-2 (위시 정리) | 2개 | 0 | 79개 | 완료 |
| 3-1~3-5 (도메인 책임) | 0 | 0 | 79개 | 완료 |
| 5-1 (옵션 수정 API) | 6개 | 0 | 82개 | 완료 |
| 5-2 (카카오 트랜잭션) | 0 | 0 | 88개 | 완료 |
