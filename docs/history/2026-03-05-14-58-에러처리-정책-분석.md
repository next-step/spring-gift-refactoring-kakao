# 에러 처리 정책 분석 및 TODO

> 작성일: 2026-03-05 14:58

## 배경

기존 코드에서 의도는 있었지만 구현이 누락된 동작을 탐색하던 중, 에러를 조용히 삼키는 패턴 2건과 테스트 누락 1건을 발견했다.

## 발견 내용

### 에러 처리 정책이 필요한 곳 (2건)

1. **`OrderEventListener.handleOrderPlaced`** — `catch (Exception ignored) {}`
   - 카카오 메시지 전송 실패 시 로깅도, 재시도도, 폴백도 없음
   - 프로젝트 전체에 Logger 사용이 전무한 상태

2. **`AuthenticationResolver.extractMember`** — `catch (Exception e) { return null; }`
   - JWT 파싱/검증 실패를 null로 반환
   - 컨트롤러마다 `if (member == null) return 401` 보일러플레이트 반복

### 테스트 누락 (1건)

3. **`MemberService.findOrCreateByEmailAndUpdateKakaoToken`**
   - 기존 회원 찾기 + 토큰 업데이트 경로
   - 신규 회원 생성 + 토큰 설정 경로
   - KakaoAuthServiceTest에서 mock으로만 사용, 실제 로직 미검증

## 주요 파일

- `src/main/java/gift/order/OrderEventListener.java` - catch (Exception ignored)
- `src/main/java/gift/auth/AuthenticationResolver.java` - catch (Exception e) return null
- `src/main/java/gift/member/MemberService.java` - findOrCreateByEmailAndUpdateKakaoToken

## 비고

- 1, 2번: TDD로 warn 로깅 추가 완료 (예외 삼킴 정책은 유지, 실패 원인 추적 가능하도록 개선)
- 3번: 테스트 추가 완료 (MemberServiceTest에 두 경로 검증), 별도 정책 불필요 확인
