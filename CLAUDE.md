# CLAUDE.md — 리팩터링 작업 가이드 (Claude Code용)

## 0. 최우선 원칙 (반드시 준수)
- **docs/2단계_요구사항.md**를 최상위 기준으로 삼는다. (상충 시 해당 문서 우선)
- 이번 단계 목표는 **작동 변경을 안전하게 수행하고, 그 결과를 증거로 보여주는 것**이다.
- 작동 변경은 반드시 **테스트 또는 검증 가능한 증거**와 함께 수행한다.
  - 예외가 발생하는지만 확인하는 것으로 충분하지 않다.
  - 상태를 재조회하거나 결과를 관찰 가능한 방식으로 검증해야 한다.
- **구조 변경과 작동 변경을 섞지 않는다.** (커밋 단위로 분리)
- 구조 변경 시 **불필요한 이름 변경을 하지 않는다.** 이름 변경이 필요하다고 판단되면 반드시 사용자에게 허락을 받는다.
- 테스트를 회피/비활성화하지 않는다. 최소 요구사항: **변경 후 전체 테스트 통과**.

## 1. 작업 진행 방식 (한 번에 몰아서 하지 말 것)
### 1.1 반드시 "단계별"로 진행
- 요구사항을 한 번에 수행하지 말고, **작은 단계로 쪼개서** 진행한다.
- 작업 단계(큰 흐름)는 `docs/plan.md`에 정리하고 계속 업데이트한다.

### 1.2 작업 시작 전 반드시 질문
- 실제 코드/파일 변경을 시작하기 전에, 항상 아래를 먼저 수행:
    1) **plan을 작성**
    2) plan을 사용자에게 보여줌
    3) **"이 plan대로 진행해도 될까요?"** 라고 물어본 뒤 승인받고 진행

> 승인 없이 코드 수정/파일 수정/대량 변경을 시작하지 않는다.

## 2. 반드시 생성/갱신해야 하는 산출물
### 2.1 README.md (프로젝트 루트)
- 작업 시작 전에 README.md에 아래를 작성한다.
    - 구현(리팩터링) 기능 목록(체크리스트)
    - 구현 전략(어떤 순서로, 어떤 기준으로)
- 작업이 추가되면 README 체크리스트도 같이 갱신한다.
- AI 도구를 활용했다면 README.md에 아래를 기록한다.
    - 활용한 방식(어디에, 무엇을)
    - Claude/AI가 만든 코드에서 무엇을 수정했는지
    - 무엇을 학습했는지(짧게라도)

### 2.2 docs/plan.md
- "전체 작업을 어떤 단계로 나눠 진행할지"를 기록한다.
  - 단계 이름
  - 목표
  - 범위(수정 파일/클래스)
  - 검증 방법(테스트/정적 분석/포매터)
  - 예상 리스크 및 롤백 전략
- 각 단계 별로 수행할 내용을 자세하게 기록한다.
  - 변경 대상 파일(예상)
  - 제약(구조 변경/작동 변경 구분)
  - 검증 명령(어떤 테스트/분석을 돌릴지)

### 2.3 docs/판단근거.md
- 요구사항에서 "근거 제시"가 필요한 판단을 모두 기록한다.
- 트랜잭션 경계 설정 시:
    - 왜 이 범위를 하나의 트랜잭션으로 묶었는지
    - 부분 반영이 발생할 수 있는 시나리오와 방지 방법
- 누락된 작동 구현 시:
    - 기존 코드에서 의도를 어떻게 확인했는지 (주석, TODO, git blame 등)
    - 구현한 작동의 검증 방법 (테스트 또는 관찰 가능한 증거)
- 도메인 책임 이동 시:
    - 무엇을 어디로 옮겼는지
    - 변경 전후의 개선 효과 (중복 제거, 호출부 단순화, 분기 감소 등)

### 2.4 ADR (Architecture Decision Record)
- 별도 파일을 만들지 않고, `docs/판단근거.md`에 함께 기록한다.
- 다음 조건 중 하나라도 해당하면 ADR 항목을 추가한다:
    - 선택지가 2개 이상이고 트레이드오프가 있었던 경우
    - 팀이 반복해서 따라야 할 규칙이나 경계를 정한 경우
    - 테스트 전략과 검증 방식이 결정의 핵심이었던 경우

## 3. 커밋 정책 (매우 중요)
- Claude Code는 **직접 커밋하지 않는다.**
- 대신, 각 단계 완료 후 **커밋 메시지 '제안'만** 한다.
- 커밋 메시지는 AngularJS Git Commit Message Conventions를 따른다.
- 한 커밋에는 목적 1개만 담는다. (diff를 30초 내 설명 못 하면 더 쪼갠다)
- **구조 변경 커밋과 작동 변경 커밋을 반드시 분리한다.**

### 커밋 메시지 제안 예시
- `refactor(order): add transaction boundary to order creation`
- `feat(order): implement wish cleanup on order completion`
- `refactor(option): move validation logic to domain entity`
- `test(order): add evidence for transaction rollback behavior`
- `docs(adr): document transaction boundary strategy`

## 4. 2단계에서 해야 할 리팩터링 범위
### 4.1 트랜잭션 경계 세우기
- 여러 저장 작업이 하나의 논리 작업이라면, 중간 실패에서 부분 반영이 발생하지 않도록 경계를 설정한다.

### 4.2 누락된 작동 구현
- 기존 코드에 의도가 남아 있었지만 구현되지 않은 작동을 완료한다.
- 새로운 작동은 반드시 테스트 또는 검증 가능한 증거로 확인한다.

### 4.3 도메인 책임 되찾기
- 작동을 유지하면서도 책임과 계산, 판단을 올바른 위치로 이동해 누수와 중복을 줄인다.
- 변경 전후가 분명한 개선을 최소 2개 이상 수행한다.
- 예: 중복 제거, 호출부 단순화, 테스트 가독성 개선, 분기/조건/반복 감소

## 5. 검증(Verification) 규칙
- 매 단계(또는 작은 변경)마다 아래를 수행하고 결과를 요약한다:
    - 전체 테스트 실행: `./gradlew test`
    - 정적 분석/린터(있다면)
    - 포매터(있다면)
- 작동 변경 시에는 **변경 전후를 비교할 수 있는 증거**를 반드시 남긴다.
- 의심되면 즉시 중단 후 사용자에게 보고/질문한다.

## 6. 보고 방식 (사용자에게 보여줄 것)
매 단계 완료 시 아래를 제공:
- 변경 요약(무슨 변경을 했는지, 구조/작동 구분)
- 영향 범위(수정 파일/클래스)
- 검증 결과(테스트/분석 결과)
- `docs/판단근거.md` 갱신 내용 요약
- 커밋 메시지 제안(직접 커밋 X) — **파일 성격이 다르면 커밋을 분리하여 제안** (docs, refactor, feat, test 등)
- `README.md` 체크리스트 갱신 (완료 항목 체크)
- 다음 단계 계획(plan) 초안 + 진행해도 되는지 질문