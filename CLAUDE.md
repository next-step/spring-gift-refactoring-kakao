# CLAUDE.md

## 워크플로우
- 커밋 전에 반드시 `./gradlew test`를 실행하여 전체 테스트 통과를 확인한다

## 단축 명령어

### "커밋" / "커밋해"
`/logical-commit` 스킬을 실행한다.

### "기록" / "기록해"
`/ai-usage-log-writer` 스킬을 실행한다. 단, 즉시 기록하지 않고 아래 절차를 따른다:

1. 현재까지의 대화와 작업 이력에서 **기록 단위(맥락)를 구분**한다
2. 구분한 맥락 목록을 사용자에게 먼저 보여주고 **검토를 요청**한다
   - 예: "다음 2개 단위로 기록하려 합니다: (1) E2E 테스트 작성 (2) Service 계층 추출 — 이대로 진행할까요?"
3. 사용자가 **승인하면** `/ai-usage-log-writer`를 실행하여 기록한다
4. 사용자가 **수정을 요청하면** 맥락을 재구성한 뒤 다시 검토를 요청한다

### "테스트 작성" / "테스트 짜줘"
`/acceptance-test-writer` 스킬을 실행한다. 대상 API를 함께 말하면 해당 API만, 미지정 시 TEST_STRATEGY.md 순서대로 진행한다.
- 예: "테스트 짜줘" → 전체 API 순서대로
- 예: "Member 테스트 짜줘" → Member API만

### "서비스 추출" / "서비스 분리해"
`/service-layer-extractor` 스킬을 실행한다. 도메인명을 함께 말하면 해당 도메인만, 미지정 시 의존 순서대로 다음 대상을 처리한다.
- 예: "서비스 분리해" → 아직 Service가 없는 다음 도메인
- 예: "Order 서비스 추출" → Order 도메인만
